generated: '2026-08-13'
method: generated
source: >-
  openapi/*.yml (operationIds verified against the specs) + AppLovin published
  documentation at support.applovin.com
note: >-
  AppLovin publishes no AGENTS.md and no skill files of its own — searched the developer
  portal, the full support.applovin.com sitemap, and the AppLovin GitHub organization.
  These four skills are generated by API Evangelist, one per marquee flow, and every
  operationId referenced was grepped out of the specifications in openapi/ rather than
  invented. Each carries the guardrails that matter most here: AppLovin has no sandbox for
  any HTTP API, no idempotency outside the Conversion API, and a Campaign Management error
  budget that blocks an account for 24 hours after 100 errors in five minutes.
skills:
- file: applovin-launch-campaign-with-creatives.md
  name: Launch an AppLovin campaign with creatives
  product: Axon Campaign Management API
  api: openapi/applovin-campaigns-api-openapi.yml
  operations: [createCampaign, uploadAssets, getAssetUploadResult, createCreativeSet, listCreativeSetsByCampaignId, listCampaigns]
  risk: high
  risk_note: Spends live advertising budget. No sandbox, no idempotency, and 4xx retries can block the account for 24 hours.
- file: applovin-configure-max-waterfall.md
  name: Configure a MAX ad unit waterfall
  product: MAX Ad Unit Management API
  api: openapi/applovin-ad-units-api-openapi.yml
  operations: [listAdUnits, getAdUnit, createAdUnit, updateAdUnit, getAdUnitWaterfall, updateAdUnitWaterfall, createTestDevice, listTestDevices]
  risk: high
  risk_note: Changes live monetization configuration and therefore live revenue. Writes are replace-semantics; read before write.
- file: applovin-send-conversion-events.md
  name: Send web conversion events to AppLovin
  product: Conversion API for lead gen
  api: openapi/applovin-conversion-events-api-openapi.yml
  operations: [postConversionEvents]
  risk: medium
  risk_note: Transmits personal data (hashed contact identifiers, IP, user agent, device advertising IDs). The only operation with a client-supplied idempotency key (dedupe_id).
- file: applovin-pull-revenue-reports.md
  name: Pull AppLovin revenue and performance reports
  product: Reporting family
  api: openapi/applovin-revenue-reporting-api-openapi.yml
  operations: [getMaxRevenueReport, getGrowthReport, getAssetReport, getAssetAnalyticsReport]
  risk: low
  risk_note: Read-only. The hazard is credential handling — the Report Key travels in the query string and lands in every access log.
cross_links:
  authentication: authentication/applovin-authentication.yml
  conventions: conventions/applovin-conventions.yml
  errors: errors/applovin-problem-types.yml
  rate_limits: rate-limits/applovin-rate-limits.yml
  sandbox: sandbox/applovin-sandbox.yml
  data_model: data-model/applovin-data-model.yml
  webhooks: asyncapi/applovin-webhooks.yml
